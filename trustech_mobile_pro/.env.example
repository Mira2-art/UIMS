# Trustech staff mobile environment sample.
# Copy this file to `.env` locally and replace placeholders with real values.
# Never commit the real `.env` file.

# Backend API base URL.
# Note: current app code still reads the base URL from ApiConfig, not this key.
BASE_URL=https://staff-api.example.com/api/v1/mobile

# Public client identifier for the staff/pro mobile app.
CLIENT_ID=trustech_mobile_staff_client

# Optional public API key placeholder if the backend enables client API keys later.
API_KEY=replace_with_staff_public_api_key_or_leave_blank
