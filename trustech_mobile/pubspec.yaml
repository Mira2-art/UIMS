name: trustech_mobile
description: "A new Flutter project."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.0+1

environment:
  sdk: ^3.10.8

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  flutter_riverpod: ^2.5.1
  dio: ^5.4.1
  flutter_dotenv: ^5.1.0
  shared_preferences: ^2.2.2
  flutter_secure_storage: ^9.0.0
  device_info_plus: ^10.1.0
  package_info_plus: ^9.0.1
  flutter_localizations:
    sdk: flutter
  intl: ^0.20.2
  go_router: ^14.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0 # Standard for newer projects
  build_runner: ^2.4.8

# Only Android & iOS are targeted (no web/linux/macOS/windows folders).
# `flutter_secure_storage` is a federated plugin: it always pulls its endorsed
# desktop/web implementations transitively (they live inside ITS pubspec, not
# ours, so they can't be deleted line-by-line). Those impls never compile into
# the Android/iOS app — EXCEPT `flutter_secure_storage_windows`, which drags in
# `path_provider` → `path_provider_android` 2.3.x, whose JNI bindings force the
# heavy `jni` NDK native build on Android. Pinning path_provider_android back to
# the channel-based 2.2.x line removes `jni`/`jni_flutter` entirely.
# Run `flutter pub get` after freeing disk space.
dependency_overrides:
  path_provider_android: '>=2.2.0 <2.3.0'

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # Generate localizations (lib/l10n/gen/app_localizations.dart) from the ARB
  # files in lib/l10n/arb using the config in l10n.yaml.
  generate: true

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # Bundle the runtime config so flutter_dotenv can read it via rootBundle.
  # Without this, dotenv.load('.env') throws FileNotFoundError at startup.
  assets:
    - .env

  # To add more assets to your application, add them under the assets section, e.g.:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # Geist is the app's brand typeface. Static weights are listed so
  # FontWeight.w100..w900 resolve precisely across all Flutter versions.
  fonts:
    - family: Geist
      fonts:
        - asset: assets/fonts/static/Geist-Thin.ttf
          weight: 100
        - asset: assets/fonts/static/Geist-ExtraLight.ttf
          weight: 200
        - asset: assets/fonts/static/Geist-Light.ttf
          weight: 300
        - asset: assets/fonts/static/Geist-Regular.ttf
          weight: 400
        - asset: assets/fonts/static/Geist-Medium.ttf
          weight: 500
        - asset: assets/fonts/static/Geist-SemiBold.ttf
          weight: 600
        - asset: assets/fonts/static/Geist-Bold.ttf
          weight: 700
        - asset: assets/fonts/static/Geist-ExtraBold.ttf
          weight: 800
        - asset: assets/fonts/static/Geist-Black.ttf
          weight: 900
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package
